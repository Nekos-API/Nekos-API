import { Callout } from "nextra-theme-docs";
import dedent from "dedent-js";

import { APIRequest } from "../../../../components/apirequest";
import {
    resource,
    stringify,
    image_ref,
    image_schema,
    artist_ref,
    artist_schema,
    character_ref,
    character_schema,
    category_ref,
    category_schema,
    user_ref,
    user_public_schema,
} from "../../../../constants/schemas";

# Character resource

## Search for a character

<APIRequest
    endpoint="/characters"
    title="Search for a character"
    description="Search for a character using a variety of different filters."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "include",
            type: "List of strings",
            description:
                "The related resources to include in the response.\n\nPossible values: `followers`, `images`",
        },
        {
            name: "filter[search]",
            type: "String",
            description: "Search for a character.",
        },
        {
            name: "filter[firstName]",
            type: "String",
            description:
                "Returns characters whose first name matches the exact specified value.",
        },
        {
            name: "filter[firstName.iexact]",
            type: "String",
            description:
                "Returns characters whose first name matches the exact specified value (case-insensitive).",
        },
        {
            name: "filter[firstName.contains]",
            type: "String",
            description:
                "Returns characters whose first name contains the specified value.",
        },
        {
            name: "filter[firstName.icontains]",
            type: "String",
            description:
                "Returns characters whose first name contains the specified value (case-insensitive).",
        },
        {
            name: "filter[firstName.startswith]",
            type: "String",
            description:
                "Returns characters whose first name starts with the specified value.",
        },
        {
            name: "filter[firstName.endswith]",
            type: "String",
            description:
                "Returns characters whose first name ends with the specified value.",
        },
        {
            name: "filter[firstName.regex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose first name matches the specified **POSIX RegEx**.",
        },
        {
            name: "filter[firstName.iregex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose first name matches the specified **POSIX RegEx** (case-insensitive).",
        },
        {
            name: "filter[lastName]",
            type: "String",
            description:
                "Returns characters whose last name matches the exact specified value.",
        },
        {
            name: "filter[lastName.iexact]",
            type: "String",
            description:
                "Returns characters whose last name matches the exact specified value (case-insensitive).",
        },
        {
            name: "filter[lastName.contains]",
            type: "String",
            description:
                "Returns characters whose last name contains the specified value.",
        },
        {
            name: "filter[lastName.icontains]",
            type: "String",
            description:
                "Returns characters whose last name contains the specified value (case-insensitive).",
        },
        {
            name: "filter[lastName.startswith]",
            type: "String",
            description:
                "Returns characters whose last name starts with the specified value.",
        },
        {
            name: "filter[lastName.endswith]",
            type: "String",
            description:
                "Returns characters whose last name ends with the specified value.",
        },
        {
            name: "filter[lastName.regex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose last name matches the specified **POSIX RegEx**.",
        },
        {
            name: "filter[lastName.iregex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose last name matches the specified **POSIX RegEx** (case-insensitive).",
        },
        {
            name: "filter[description]",
            type: "String",
            description:
                "Returns characters whose description matches the exact specified value.",
        },
        {
            name: "filter[description.iexact]",
            type: "String",
            description:
                "Returns characters whose description matches the exact specified value (case-insensitive).",
        },
        {
            name: "filter[description.contains]",
            type: "String",
            description:
                "Returns characters whose description contains the specified value.",
        },
        {
            name: "filter[description.icontains]",
            type: "String",
            description:
                "Returns characters whose description contains the specified value (case-insensitive).",
        },
        {
            name: "filter[description.startswith]",
            type: "String",
            description:
                "Returns characters whose description starts with the specified value.",
        },
        {
            name: "filter[description.endswith]",
            type: "String",
            description:
                "Returns characters whose description ends with the specified value.",
        },
        {
            name: "filter[description.regex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose description matches the specified **POSIX RegEx**.",
        },
        {
            name: "filter[description.iregex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose description matches the specified **POSIX RegEx** (case-insensitive).",
        },
        {
            name: "filter[gender]",
            type: "String",
            description:
                "Returns characters whose gender matches the exact specified value.",
        },
        {
            name: "filter[gender.iexact]",
            type: "String",
            description:
                "Returns characters whose gender matches the exact specified value (case-insensitive).",
        },
        {
            name: "filter[gender.contains]",
            type: "String",
            description:
                "Returns characters whose gender contains the specified value.",
        },
        {
            name: "filter[gender.icontains]",
            type: "String",
            description:
                "Returns characters whose gender contains the specified value (case-insensitive).",
        },
        {
            name: "filter[gender.startswith]",
            type: "String",
            description:
                "Returns characters whose gender starts with the specified value.",
        },
        {
            name: "filter[gender.endswith]",
            type: "String",
            description:
                "Returns characters whose gender ends with the specified value.",
        },
        {
            name: "filter[gender.regex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose gender matches the specified **POSIX RegEx**.",
        },
        {
            name: "filter[gender.iregex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose gender matches the specified **POSIX RegEx** (case-insensitive).",
        },
        {
            name: "filter[species]",
            type: "String",
            description:
                "Returns characters whose species matches the exact specified value.",
        },
        {
            name: "filter[species.iexact]",
            type: "String",
            description:
                "Returns characters whose species matches the exact specified value (case-insensitive).",
        },
        {
            name: "filter[species.contains]",
            type: "String",
            description:
                "Returns characters whose species contains the specified value.",
        },
        {
            name: "filter[species.icontains]",
            type: "String",
            description:
                "Returns characters whose species contains the specified value (case-insensitive).",
        },
        {
            name: "filter[species.startswith]",
            type: "String",
            description:
                "Returns characters whose species starts with the specified value.",
        },
        {
            name: "filter[species.endswith]",
            type: "String",
            description:
                "Returns characters whose species ends with the specified value.",
        },
        {
            name: "filter[species.regex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose species matches the specified **POSIX RegEx**.",
        },
        {
            name: "filter[species.iregex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose species matches the specified **POSIX RegEx** (case-insensitive).",
        },
        {
            name: "filter[nationality]",
            type: "String",
            description:
                "Returns characters whose nationality matches the exact specified value.",
        },
        {
            name: "filter[nationality.iexact]",
            type: "String",
            description:
                "Returns characters whose nationality matches the exact specified value (case-insensitive).",
        },
        {
            name: "filter[nationality.contains]",
            type: "String",
            description:
                "Returns characters whose nationality contains the specified value.",
        },
        {
            name: "filter[nationality.icontains]",
            type: "String",
            description:
                "Returns characters whose nationality contains the specified value (case-insensitive).",
        },
        {
            name: "filter[nationality.startswith]",
            type: "String",
            description:
                "Returns characters whose nationality starts with the specified value.",
        },
        {
            name: "filter[nationality.endswith]",
            type: "String",
            description:
                "Returns characters whose nationality ends with the specified value.",
        },
        {
            name: "filter[nationality.regex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose nationality matches the specified **POSIX RegEx** (case-sensitive).",
        },
        {
            name: "filter[nationality.iregex]",
            type: "POSIX RegEx",
            description:
                "Returns characters whose nationality matches the specified **POSIX RegEx** (case-insensitive).",
        },
        {
            name: "filter[createdAt]",
            type: "ISO 8601",
            description:
                "Returns characters created at a certain date and time.",
        },
        {
            name: "filter[createdAt.second]",
            type: "Number (0-59)",
            description: "Returns characters created at a specific second.",
        },
        {
            name: "filter[createdAt.minute]",
            type: "Number (0-59)",
            description: "Returns characters created at a specific minute.",
        },
        {
            name: "filter[createdAt.hour]",
            type: "Number (0-23)",
            description: "Returns characters created at a specific hour.",
        },
        {
            name: "filter[createdAt.day]",
            type: "Number (1-31)",
            description:
                "Returns characters created at a specific day of the month.",
        },
        {
            name: "filter[createdAt.month]",
            type: "Number (1-12)",
            description: "Returns characters created during a specific month.",
        },
        {
            name: "filter[createdAt.year]",
            type: "Number (0-2023)",
            description: "Returns characters created during a spcific year.",
        },
        {
            name: "filter[updatedAt]",
            type: "ISO 8601",
            description:
                "Returns characters created at a certain date and time.",
        },
        {
            name: "filter[updatedAt.second]",
            type: "Number (0-59)",
            description: "Returns characters created at a specific second.",
        },
        {
            name: "filter[updatedAt.minute]",
            type: "Number (0-59)",
            description: "Returns characters created at a specific minute.",
        },
        {
            name: "filter[updatedAt.hour]",
            type: "Number (0-23)",
            description: "Returns characters created at a specific hour.",
        },
        {
            name: "filter[updatedAt.day]",
            type: "Number (1-31)",
            description:
                "Returns characters created at a specific day of the month.",
        },
        {
            name: "filter[updatedAt.month]",
            type: "Number (1-12)",
            description: "Returns characters created during a specific month.",
        },
        {
            name: "filter[updatedAt.year]",
            type: "Number (0-2023)",
            description: "Returns characters created during a spcific year.",
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(pagination([character_schema])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

## Get a character by its ID

<APIRequest
    endpoint="/characters/:id"
    title="Get a character by its ID"
    description="Get a character's details by its ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The character's ID.",
            location: "path",
            required: true,
        },
        {
            name: "include",
            type: "List of strings",
            description:
                "The related resources to include in the response.\nPossible values: images,followers",
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource(character_schema)),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

## Follow a character

<APIRequest
    method="POST"
    endpoint="/characters/:id/follow"
    title="Follow a character"
    description="Follows a character by its ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
        {
            name: "Content-Type",
            description: "application/vnd.api+json",
            required: true,
        },
        {
            name: "Authorization",
            description: "Bearer &lt;access-token&gt;",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID",
            description: "The character's ID.",
            required: true,
            location: "path",
        },
    ]}
    responses={[
        {
            code: 204,
            name: "Success",
            description: "The request was completed successfully.",
        },
    ]}
/>

<APIRequest
    method="DELETE"
    endpoint="/characters/:id/follow"
    title="Unfollow a character"
    description="Unfollows a character by its ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
        {
            name: "Content-Type",
            description: "application/vnd.api+json",
            required: true,
        },
        {
            name: "Authorization",
            description: "Bearer &lt;access-token&gt;",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID",
            description: "The character's ID.",
            required: true,
            location: "path",
        },
    ]}
    responses={[
        {
            code: 204,
            name: "Success",
            description: "The request was completed successfully.",
        },
    ]}
/>
