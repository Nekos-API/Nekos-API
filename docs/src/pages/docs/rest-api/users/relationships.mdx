import { Callout } from "nextra-theme-docs";

import { APIRequest } from "../../../../components/apirequest";
import {
    resource,
    stringify,
    image_ref,
    image_schema,
    artist_ref,
    artist_schema,
    character_ref,
    character_schema,
    category_ref,
    category_schema,
    user_ref,
    user_public_schema,
} from "../../../../constants/schemas";

# User Relationships

These endpoints return resources related to the user resource, such as it's saved images or its followers.

<Callout type="warning">
    None of these endpoints supports pagination. They all return all resources
    at once, which whill probaby cause the request to be <b>extremely slow</b>.
</Callout>

## Get a user's followers

<APIRequest
    endpoint="/users/:id/followers"
    title="Get a user's followers by the user's ID"
    description="Get a user's followers by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([user_public_schema])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

<APIRequest
    endpoint="/users/:id/relationships/followers"
    title="Get a user's followers relationship by the user's ID"
    description="Get a user's followers relationship by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([user_ref])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
        {
            code: 200,
            name: "Success, no resource",
            description:
                "The request was completed successfully but the user has no followers",
            example: {
                code: stringify(resource([])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

## Get a user's followed users

<APIRequest
    endpoint="/users/:id/following"
    title="Get a user's followed users by the user's ID"
    description="Get a user's followed users by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([user_public_schema])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

<APIRequest
    endpoint="/users/:id/relationships/following"
    title="Get a user's followed users relationship by the user's ID"
    description="Get a user's followed users relationship by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([user_ref])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
        {
            code: 200,
            name: "Success, no resource",
            description:
                "The request was completed successfully but the user is not following anyone",
            example: {
                code: stringify(resource([])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

## Get a user's liked images

<APIRequest
    endpoint="/users/:id/liked-images"
    title="Get a user's liked images by the user's ID"
    description="Get a user's liked images by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([image_schema])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

<APIRequest
    endpoint="/users/:id/relationships/liked-images"
    title="Get a user's liked images relationship by the user's ID"
    description="Get a user's liked images relationship by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([image_ref])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
        {
            code: 200,
            name: "Success, no resource",
            description:
                "The request was completed successfully but the user has no liked images",
            example: {
                code: stringify(resource([])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

## Get a user's saved images

<APIRequest
    endpoint="/users/:id/saved-images"
    title="Get a user's saved images by the user's ID"
    description="Get a user's saved images by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([image_schema])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

<APIRequest
    endpoint="/users/:id/relationships/saved-images"
    title="Get a user's saved images relationship by the user's ID"
    description="Get a user's saved images relationship by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([image_ref])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
        {
            code: 200,
            name: "Success, no resource",
            description:
                "The request was completed successfully but the user has no saved images",
            example: {
                code: stringify(resource([])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

## Get a user's followed artists

<APIRequest
    endpoint="/users/:id/followed-artists"
    title="Get a user's followed artists by the user's ID"
    description="Get a user's followed artists by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([artist_schema])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

<APIRequest
    endpoint="/users/:id/relationships/followed-artists"
    title="Get a user's followed artists relationship by the user's ID"
    description="Get a user's followed artists relationship by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([artist_ref])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
        {
            code: 200,
            name: "Success, no resource",
            description:
                "The request was completed successfully but the user is not following any artists",
            example: {
                code: stringify(resource([])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

## Get a user's followed characters

<APIRequest
    endpoint="/users/:id/followed-characters"
    title="Get a user's followed characters by the user's ID"
    description="Get a user's followed characters by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([character_schema])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

<APIRequest
    endpoint="/users/:id/relationships/followed-characters"
    title="Get a user's followed characters relationship by the user's ID"
    description="Get a user's followed characters relationship by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([character_ref])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
        {
            code: 200,
            name: "Success, no resource",
            description:
                "The request was completed successfully but the user is not following any characters",
            example: {
                code: stringify(resource([])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

## Get a user's followed categories

<APIRequest
    endpoint="/users/:id/followed-categories"
    title="Get a user's followed categories by the user's ID"
    description="Get a user's followed categories by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([category_schema])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>

<APIRequest
    endpoint="/users/:id/relationships/followed-categories"
    title="Get a user's followed categories relationship by the user's ID"
    description="Get a user's followed categories relationship by the user's ID."
    headers={[
        {
            name: "Accept",
            description: "application/vnd.api+json",
            required: true,
        },
    ]}
    parameters={[
        {
            name: "id",
            type: "UUID v4",
            description: "The user's ID.",
            location: "path",
            required: true,
        },
    ]}
    responses={[
        {
            code: 200,
            name: "Success",
            description: "The request was completed successfully.",
            example: {
                code: stringify(resource([category_ref])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
        {
            code: 200,
            name: "Success, no resource",
            description:
                "The request was completed successfully but the user is not following any categories",
            example: {
                code: stringify(resource([])),
                language: "JSON",
                headers: ["Content-Type: application/vnd.api+json"],
            },
        },
    ]}
/>
