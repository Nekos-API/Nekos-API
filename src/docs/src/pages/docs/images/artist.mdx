import { Tabs } from "nextra-theme-docs";

# Get an Image's Artist

This endpoint allows you to get an image's artist.

## Endpoint Details

-   URL: `/v3/images/{id}/artist`
-   Method: `GET`
-   Response: `JSON`

## Parameters

| Location | Name | Type    | Description     | Required |
| -------- | ---- | ------- | --------------- | -------- |
| Path     | `id` | Integer | The image's ID. | Yes      |

## Example Request

<Tabs items={["Python 3", "JavaScript"]}>
    <Tabs.Tab>
        ```python filename="image_artist.py" showLineNumbers
        import requests

        res = requests.get(f"https://api.nekosapi.com/v3/images/{image_id}/artist")
        res.raise_for_status()

        data = res.json()
        ```
    </Tabs.Tab>
    <Tabs.Tab>
        ```javascript filename="image_artist.js" showLineNumbers
        fetch(`https://api.nekosapi.com/v3/images/${image_id}/artist`, {
            method: "GET",
        }).then(res => res.json())
        .then(data => {
            console.log(data);
        })
        ```
    </Tabs.Tab>

</Tabs>
